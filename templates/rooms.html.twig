{% extends 'base.html.twig' %}

{% block title %}Učebny{% endblock %}

{% block page %}rooms{% endblock %}

{% block content %}
    <h1>Učebny</h1>

    {% if rooms is empty %}

        <p>Nebyly nalezeny žádné učebny. Pro zobrazení neveřejných učeben je potřeba být přihlášen správným účtem.</p>

    {% else %}

        <table>
            <thead>
                <tr>
                    <th>Učebna</th>
                    <th>Budova</th>
                    <th>Stav</th>
                    <th>Akce</th>
                </tr>
            </thead>
            <tbody>

            </tbody>
            {% for room in rooms %}
                <tr>
                    <td>{{ room.getFullName() }}</td>
                    <td>{{ room.getBuilding() }}</td>
                    <td>
                        {% if currentAvailabilityMap[room.getId()] %}
                            Volná
                        {% else %}
                            Obsazená
                        {% endif %}
                    </td>
                    <td>
                        <a href="{{ path('app_room_book', {'roomUrlName': room.getUrlName()}) }}">
                            Rezervovat
                        </a>
                    </td>
                </tr>
            {% endfor %}
        </table>

    {% endif %}


{% endblock %}
